## What changed

Describe the user-visible problem and the resulting behavior.

## Validation

- [ ] `cargo test --locked`
- [ ] `cargo clippy --locked --all-targets -- -D warnings`
- [ ] `cargo fmt --check`
- [ ] `npm ci && npm run check && npm run format:check`
- [ ] I tested the affected workflow in a real Tauri window when it changes interaction behavior.

